import { GraphQLList } from 'graphql';
import query from '../../utils';
import CountryCluster from './ContryCluster';
import { Context } from '../../Types';

const {{name}} = {
    type: GraphQLList(CountryCluster),
    resolve(object, arguments_, context: Context) {
        return query('SELECT * FROM [Registration].[CountryCluster]', context.sqlInstance);
    }
};
export default {{name}};
